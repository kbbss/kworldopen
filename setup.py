from setuptools import setup, find_packages

setup(
   name='klswordopen',
   version='0,9,1',
   description='Something different',
   author='kebiat',
   author_email='kebiat@naver.com',
   packages=['klsword','osul'],  # would be the same as name

)